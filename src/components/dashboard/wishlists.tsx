'use client';

import { useRouter } from 'next/navigation';

const Wishlists = () => {
  const router = useRouter;

  return (
    <main>
      <div></div>
    </main>
  );
};

export default Wishlists;
